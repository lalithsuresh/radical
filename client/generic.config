# This config file contains generic information used when a client 
# is spawned from the Puppet Master. 


# list of all servers
central-1 tcp://127.0.0.1:8080
central-2 tcp://127.0.0.1:8081
central-3 tcp://127.0.0.1:8082

# Debug list
#client.Client all
#client.CalendarServiceClient all
comm.SendReceiveMiddleLayer all
#client.PuppetClientService all


# To faciliatate debugging,
# we can haz dummy recipient
# who'll simply respond with
# whatever messages we 'want'

#dummyrecipient true
puppetmaster tcp://127.0.0.1:10000
